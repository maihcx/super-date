export interface SuperDateOptions {
    /** Default display format, e.g. "dd/MM/yyyy". Can be overridden per-element
     *  via the `data-date-format` attribute. */
    format?: string;
    /** Locale used for month/weekday names. Defaults to navigator.language. */
    locale?: string;
}
//# sourceMappingURL=super-date.type.d.ts.map